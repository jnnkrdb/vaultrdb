{{- if .Values.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  annotations:
    helm.sh/hook: pre-install
    helm.sh/hook-weight: "5"  
  labels:
    {{- include "helmbase.labels" . | nindent 4 }}
{{- end }}